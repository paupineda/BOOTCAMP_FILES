pfolder('Sample Folder') {
    displayName('Sample Folder')
  description('Folder generated by a DSL Script')
}
job('Sample Folder/Job Inside Folder')
{
    wrappers
    {
        preBuildCleanup()
    }
    scm
    {
git('linktorepo', 'master')
    }
    triggers
    {
        scm('* * * * *')
    }
    steps
    {
    shell('javac HelloWorld.java')
    }
    publishers 
    {
    downstream('Job Outside Folder', 'SUCCESS')
    }
}
job('Job Outside Folder')
{
  customWorkspace('jobs/Job Inside Folder/workspace')
  steps
  {
    shell('java HelloWorld')
  }
}
buildPipelineView('Test Pipeline') 
{
    title('Poll SCM Sample')
    displayedBuilds(5)
    selectedJob('Sample Folder/Job Inside Folder')
    alwaysAllowManualTrigger()
    showPipelineParameters()
    refreshFrequency(60)
}